# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # The site embeds data fetched at build time (theorem counts, contributor
  # counts), so it goes stale without a periodic rebuild even when nothing in
  # this repository changes.
  schedule:
    - cron: '0 4 * * *'

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          # Pinned rather than required: 3.11, 3.13 and 3.14 were checked to
          # produce byte-identical output from the same requirements.txt.
          python-version: '3.11'

      - name: Install bibtool
        run: |
          sudo apt-get update --fix-missing
          sudo apt-get install -y bibtool

      - name: Install Python dependencies
        run: python -m pip install --upgrade pip -r community/requirements.txt

      - name: Build
        # Deploying replaces the whole site, so a partial build must never
        # be deployed; letting the build fail here leaves the previous
        # deployment serving.
        run: ./build.sh
        env:
          # Needed for the GitHub API calls made while building; unauthenticated
          # requests rate-limit.
          GITHUB_TOKEN: ${{ github.token }}
          # Currently unread: the Zulip scrape they feed is commented out in
          # make_site.py along with meet.html, the only page that used it.
          # Kept so the plumbing is here if that page is migrated. The address
          # is a variable rather than a secret because it is not sensitive, but
          # both are needed to authenticate; see README.md.
          MAP_ZULIP_EMAIL: ${{ vars.MAP_ZULIP_EMAIL }}
          MAP_ZULIP_KEY: ${{ secrets.MAP_ZULIP_KEY }}
          # Optional. Without it the reviewers team page omits review interests.
          QUEUEBOARD_REVIEWER_INTERESTS_API_URL: ${{ vars.QUEUEBOARD_REVIEWER_INTERESTS_API_URL }}

      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
